<h1>Games</h1>

<table>
  <thead>
    <tr>
    </tr>
  </thead>

  <tbody>
    <% @games.each do |game| %>
      <tr>
        <td>
          <h3><%= link_to game.title, game %></h3>
          <%= image_tag(game.img_url, width:'100px') %><br>
          <%= game.year %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>