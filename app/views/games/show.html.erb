<div class="game">
<%= content_for(:title, @game.title) %>

<div class="col-sm-6 contain profile">
	<%= render 'profile' %>
	<%= render 'controls' %>
	<%= render 'stats' %>
	<div class="clearfix"></div>
	<hr>
	<p class="description">
  		<strong>Description:</strong>
  		<%= @game.description %>
	</p>
</div>

<div class="feed col-sm-6">

<div id="posts">
	<%= render @posts %>
</div>

<div id="infinite-scrolling">
	<%= will_paginate @posts %>
</div>

</div>
</div>
<%= render 'modal' if current_user %>