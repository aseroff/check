<%= content_for(:title, "Who " + @user.username + " is following") %>
<div class="col-sm-4">

<div class="no-mobile fixed contain profile">
<%= render 'profile' %>
<%= render 'controls' %>
</div>

<div class="only-mobile contain profile">
<%= render 'profile' %>
<%= render 'controls' %>
</div>

</div>

<div class="feed col-sm-8">  
<h3 class="col-sm-12">Following</h3>

<div id="user-list" class="col-sm-12">
<%= render @users %>
</div>

<div id="infinite-scrolling">
	<%= will_paginate @users %>
</div>

<div class="center">
	<%= link_to 'Find more friends to follow here', users_path %>
</div>

</div>