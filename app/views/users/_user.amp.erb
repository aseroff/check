<div class="list-user contain center">
    <%= link_to image_tag(user.avatar.thumb.to_s, size: "50", class: 'circle user-image'), user_path(user, format: :amp) %>
    <h3><%= link_to user.display_name, user_path(user, format: :amp) %></h3>
</div>