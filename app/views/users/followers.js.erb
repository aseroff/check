<% @relations.each do |r| %>
   <% user = r.user %>
   $('#user-list').append('<%= j render user %>');
<% end %>

<% if @users.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @users %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>