<div class="profile col-sm-3">
<% cache @user do %>
<%= render 'profile' %>
<% end %>
</div>

<div class="feed col-sm-9">
    <h3>Check-ins</h3>

	<div id="posts">
    <%= render @posts %>
    </div>

	<div id="infinite-scrolling">
  		<%= will_paginate @posts %>
	</div>
</div>