<%= content_for(:title, @user.username + "'s owned games") %>

<div class="col-sm-4">

<div class="no-mobile fixed contain profile">
<%= render 'profile' %>
<%= render 'controls' %>
</div>

<div class="only-mobile contain profile">
<%= render 'profile' %>
<%= render 'controls' %>
</div>

</div>

<div class="feed col-sm-8">  
<h3 class="col-sm-12">Owned Games</h3>

<div id="game-list" class="col-sm-12">
<%= render @games %>
</div>

<div id="infinite-scrolling">
	<%= will_paginate @games %>
</div>
</div>